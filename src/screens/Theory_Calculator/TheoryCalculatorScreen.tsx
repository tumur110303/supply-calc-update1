import { FC } from "react";
import { View, StyleSheet, Text } from "react-native";

const TheoryCalculatorScreent: FC = (props) => {
  return (
    <View>
      <Text>Цахилгаан хэлхээний онол...</Text>
    </View>
  );
};

export default TheoryCalculatorScreent;

const css = StyleSheet.create({
  container: {},
});
